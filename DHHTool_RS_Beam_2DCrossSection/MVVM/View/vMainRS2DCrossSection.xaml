<Window
    x:Class="DHHTool_RS_Beam_2DCrossSection.MVVM.View.vMainRS2DCrossSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DHHTool_RS_Beam_2DCrossSection.MVVM.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary Source="pack://application:,,,/DHHLibrary;component/Themes/Themes.xaml" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="42" />
        </Grid.RowDefinitions>
        <DataGrid
            AlternatingRowBackground="AliceBlue"
            AlternationCount="2"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ItemsSource="{Binding DgSectionBeam}">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Binding="{Binding BeamName}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="Name" />
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTemplateColumn Width="120" Header="Chọn dầm">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button
                                    Command="{Binding CreateSectionDetail}"
                                    CommandParameter="{Binding ElementName=CreateSectionWin}"
                                    Content="Sửa"
                                    Style="{DynamicResource DHHButtonStyle}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="1">
            <Button
                Command="{Binding CreateSectionDetail}"
                CommandParameter="{Binding ElementName=CreateSectionWin}"
                Content="OK"
                DockPanel.Dock="Right"
                Style="{DynamicResource DHHButtonStyle}" />
            <Button
                HorizontalAlignment="Right"
                Command="{Binding FileExcelOpen}"
                Content="Tệp Excel"
                Style="{DynamicResource DHHButtonStyle}" />
        </DockPanel>
    </Grid>
</Window>
